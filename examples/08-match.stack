@import "stdlib"

func main () (int) in
    0
    string.sizeof array.init.with_sz
    dup "string\n" string.& array.append unwrap

    match num xs in
        num int.show string.stdout "\n" string.stdout
        xs array.count int.show string.stdout "\n" string.stdout
        xs num array.get unwrap
        string.* string.stdout
    end

    0
end
